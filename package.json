{
  "name": "ftir-analysis-server",
  "private": true,
  "version": "0.1.0",
  "description": "Server-side FTIR interpretation proxy",
  "scripts": {
    "start": "node --env-file=.env server.js"
  },
  "engines": {
    "node": ">=20.6"
  }
}
